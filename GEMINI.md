# Project Instructions

## Repository Management
- The `noclaude/` directory is a tool used for removing references to Claude from the project.
- It must be kept out of the main repository (it is added to `.gitignore`).
